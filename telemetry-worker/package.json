{
  "name": "vibeready-telemetry-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test test/worker-contract.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply vibeready-telemetry-dev --local",
    "migrate:remote": "wrangler d1 migrations apply vibeready-telemetry-prod --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260703.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
